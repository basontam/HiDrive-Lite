# Demo and test fixtures

The bootstrap script creates a small fictional library in memory and installs
it into the local encrypted database. Test workbooks and links under
`tests/fixtures/` use `swfake`/`fake` share codes and must never be replaced by
real resources.
