# Development / test-only dependencies.  Production dependencies live in
# requirements.txt and must not be changed here.
-r requirements.txt
pytest==9.1.1
openpyxl==3.1.5
pypinyin==0.55.0
zhconv==1.4.3
playwright==1.62.0
